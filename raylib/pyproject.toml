[project]
name = "raylib"
version = "0.0.0"
description = "Build script for raylib Python bindings (electronstudio/raylib-python-cffi)"
requires-python = ">=3.8"
dependencies = ["cffi>=1.15.1", "setuptools", "wheel"]
